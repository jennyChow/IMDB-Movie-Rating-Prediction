ml-100k is our data, which is the data we are using. 

Each folder starting with names contains our source code. Inside each folder, we also have a README which shows the detailed instruction to compile and run the code. 